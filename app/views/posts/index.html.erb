<% provide(:title, "Club House") %>
<h1>Clubhouse</h1>
<% @post.each do |post| %>
      <tr>
        <td><%= post.body %></td>
        <% if logged_in? %>
        	<td> | <%= post.user.username %></td>
        <% end %>
  		</tr>
<% end %>

<%= link_to "New Post", new_post_path %>